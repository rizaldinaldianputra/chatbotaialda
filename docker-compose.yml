version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend/chat.db:/app/chat.db

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
